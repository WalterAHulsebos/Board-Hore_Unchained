using UnityEngine;

namespace CommonGames.Utilities.CGTK.Greasy
{
    public static class Vector3IntTweens
    {
        /*
        public static Coroutine To(this Vector3Int from, Vector3Int to, float duration, EaseType ease)
            => Greasy.To(from, to, duration, ease, setter:(x => from = x));
		
        public static Coroutine To(this Vector3Int from, Vector3Int to, float duration, EaseMethod ease)
            => Greasy.To(from, to, duration, ease, setter:(x => from = x));
        */
    }
}